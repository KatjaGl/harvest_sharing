var gardenId = '<%= @garden.id%>'
function toggleLikeButton(gardenId, innerHTML) {
  let likeButton = document.querySelector(`.like-icon[data-id='${gardenId}']`)
  likeButton.classList.toggle('red');
}
toggleLikeButton(gardenId);
